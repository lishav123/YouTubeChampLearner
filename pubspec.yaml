name: yt_champ_learner
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  flutter_inappwebview: ^6.1.5
  intl: ^0.20.2
  provider: ^6.1.5+1
  shared_preferences: ^2.5.5
  shelf: ^1.4.2
  youtube_explode_dart: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
